<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/coal</item>
    </style>

    <style name="RmsTabLayout" parent="Widget.Design.TabLayout">
        <item name="android:background">@color/steel</item>
        <item name="tabIndicatorColor">@color/olive_dark</item>
        <item name="tabTextAppearance">@style/RmsTabTextAppearance</item>
        <item name="tabSelectedTextColor">@color/olive_dark</item>
    </style>
    <style name="RmsTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">10dp</item>
        <item name="android:textColor">@color/coal</item>
        <item name="textAllCaps">true</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>
